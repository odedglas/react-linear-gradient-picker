import React, { useReducer, useRef } from 'react';
import PropTypes from 'prop-types';
import './style.css';

const App = () => {

    return (
        <div> HEssssY </div>
    );
};

export default App;

App.propTypes = {};
